# dependency ของ backend (API ที่ deploy ขึ้น server)
# สร้างจากรายการ import จริงในโค้ด ไม่ใช่ pip freeze ทั้ง venv
# ของที่ใช้เฉพาะตอนเทรน (matplotlib, scikit-learn) อยู่ใน training/requirements-train.txt

# ===== เว็บเซิร์ฟเวอร์ =====
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4

# ===== ฐานข้อมูล =====
SQLAlchemy==2.0.51
psycopg2-binary==2.9.12      # ไดรเวอร์ Postgres สำหรับ DATABASE_URL
python-dotenv==1.2.2

# ===== ยืนยันตัวตน =====
python-jose[cryptography]==3.5.0   # สร้าง/ตรวจ JWT
passlib==1.7.4                     # เข้ารหัสรหัสผ่าน
bcrypt==4.0.1                      # อัลกอริทึมที่ passlib เรียกใช้

# ===== ข้อมูลหุ้น =====
yfinance==1.5.2
pandas==3.0.5

# ===== โมเดลพยากรณ์ =====
# ใช้ ONNX Runtime แทน TensorFlow เพราะที่นี่ทำแค่ทำนาย ไม่ได้เทรน
#   tensorflow + keras + clang = 1,512 MB   <- ใหญ่เกิน free tier
#   onnxruntime                =    43 MB
# การเทรนยังใช้ TensorFlow เหมือนเดิม แต่เกิดบน GitHub Actions ที่ทรัพยากรเหลือเฟือ
# แล้วแปลงเป็น .onnx ก่อน commit (ดู training/train.py)
onnxruntime==1.28.0
# โมเดล XGBoost เก็บเป็น .json ผ่าน save_model/load_model ของ xgboost เอง
# ไม่ได้ใช้ pickle เพราะ pickle ของ XGBoost ย้ายข้ามแพลตฟอร์มไม่ได้
# (ทดสอบแล้วว่าไม่ต้องมี scikit-learn เพราะ xgboost มี fallback ให้)
xgboost==3.4.0
