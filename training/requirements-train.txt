# dependency สำหรับเทรนโมเดลเท่านั้น (รันบน GitHub Actions)
# แยกจาก requirements.txt ของ backend เพราะฝั่ง deploy ไม่ควรมี TensorFlow ติดไปด้วย
tensorflow==2.21.0
keras==3.15.1
keras-tcn==3.5.6
# แปลงโมเดลที่เทรนเสร็จเป็น .onnx ให้ฝั่ง deploy ใช้โดยไม่ต้องมี TensorFlow
tf2onnx==1.17.0
onnxruntime==1.28.0          # ใช้ตรวจว่าไฟล์ที่แปลงแล้วให้ผลตรงกับต้นฉบับ
xgboost==3.4.0
scikit-learn==1.9.0
pandas==3.0.5
numpy==2.5.1
yfinance==1.5.2
