# ===== Python =====
venv/
__pycache__/
*.py[cod]
.env

# ===== Node / frontend =====
node_modules/
dist/
.vite/

# ===== ผลพลอยได้จากการเทรน =====
# ไฟล์โมเดลใน backend/model/ ต้อง commit ขึ้น git (workflow ใช้ส่งโมเดลไป deploy)
# แต่รูปกราฟกับ log ไม่ต้อง
training/plots/
training/*.png
*.log

# ===== ระบบปฏิบัติการ / เครื่องมือ =====
.DS_Store
Thumbs.db
.idea/
.vscode/
# เครื่องมือช่วยเขียนโค้ด — เป็นการตั้งค่าเฉพาะเครื่อง ไม่ใช่ส่วนหนึ่งของโปรเจกต์
# หมายเหตุ: ถ้าวันหลังสร้าง CLAUDE.md ไว้บันทึกข้อตกลงของโปรเจกต์
# ไฟล์นั้นอยู่ที่ราก ไม่ได้อยู่ในโฟลเดอร์นี้ จึงยัง commit ได้ตามปกติ
.claude/
